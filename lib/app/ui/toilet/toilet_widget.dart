import 'package:flutter/material.dart';

class ToiletWidget extends StatelessWidget {
  const ToiletWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
